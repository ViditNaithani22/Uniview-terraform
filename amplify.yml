version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Nothing to build. This is a static website."
  artifacts:
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths: []
